describe('index', () => {
  it('works', () => {
    expect(true).toEqual(true);
  });

  // it.each([{ test: true, expected: true }])(
  //   'returns the correct value',
  //   ({ test, expected }) => {
  //     expect(test).toEqual(expected);
  //   },
  // );
});
